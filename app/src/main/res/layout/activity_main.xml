<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">


    <!-- NavHostFragment para manejar la navegación entre fragmentos -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/navHostFragment"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:defaultNavHost="true"
        app:layout_constraintBottom_toTopOf="@id/navView"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:navGraph="@navigation/nav_graph" />

    <!-- BottomNavigationView para la navegación inferior -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/navView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:menu="@menu/bottom_nav_menu" />

    <!--
             Esta vista será la capa SUPERPUESTA (overlay) del tutorial.
             No sustituye la pantalla: simplemente se coloca ENCIMA de ella.

             Fondo semitransparente (#88 = 50% de opacidad).
             Esto oscurece la pantalla inferior para centrar la atención.

             android:visibility="gone":
             visible → se ve y ocupa espacio
             invisible → no se ve, pero sigue ocupando espacio
             gone → no se ve y además no ocupa espacio
             Lo dejamos oculto al inicio. Solo se mostrará cuando el tutorial esté activo.

               clickable + focusable = el overlay captura los toques.
              Esto evita que el usuario toque elementos de debajo
              mientras el tutorial está activo.
              -->
    <FrameLayout
        android:id="@+id/overlayGuia"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#88000000"
        android:visibility="gone"
        android:clickable="true"
        android:focusable="true"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>


</androidx.constraintlayout.widget.ConstraintLayout>